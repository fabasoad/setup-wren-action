---
name: Merge PR

on:
  check_suite:
    types: [completed]

jobs:
  print_hello:
    name: Print Hello
    runs-on: ubuntu-latest
    if: ${{ github.ref_name == 'pre-commit-ci-update-config' }}
    steps:
      - name: Print
        run: echo 'Hello ${{ toJSON(job) }}!'
        shell: bash
      - name: Test
        uses: actions/github-script@v6
        with:
          script: |
            console.log(context);
            console.log('-------!--------');
#            const res = await github.rest.checks.getSuite({
#              owner: context.repo.owner,
#              repo: context.repo.repo,
#              check_suite_id: context.payload.check_suite.id,
#            });
            const res = await github.rest.checks.listForRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: context.ref
            });
            console.log(res);
